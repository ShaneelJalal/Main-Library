#List Compressions
# Given an array of integers, return a new array where each element in the new array is the number

numbers = [1, 2, 3, 4, 5,]
numbers_power_2 = [n**2 for n in numbers]
print(numbers_power_2)

#Polymorphism
# Given a list of numbers, return a new list where each element is the square of the original list

class Dog:
  def eat(self):
    print("nom nom")

class Cat:
  def eat(self):
    print("nom nom")

animal1 = Dog()
animal2 = Cat()

animal1.eat()
animal2.eat()
